--  SUBQUERIES 

-- A subquery is a query written inside another SQL query.


/*Basic Syntax

SELECT column_name
FROM table_name
WHERE column_name OPERATOR (
    SELECT column_name
    FROM table_name
  WHERE condition
);

*/

-- Find customers whose salary is greater than the average salary of all customers.

-- Find customers whose salary is equal to the highest salary.


-- Find customers whose salary is equal to the lowest salary

-- Check whether the company has more than 1000 customers.


-- Find customers who have placed an order.

-- Find customers whose salary is greater than the average salary.


-- Find the customer(s) having the maximum salary.

-- Find cities whose average salary is greater than the overall average salary.

-- Find customers whose salary is greater than the average salary of their own city.

